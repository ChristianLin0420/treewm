# @package model
# Fast-iteration variant for smoke tests and unit tests.
z_dim: 64
q_dim: 32
hidden_dim: 128
encoder_hidden: 128
num_layers: 2
num_heads: 4
branch_factor: 4
h_max: 64
horizons: [4, 8, 16, 32, 64]
scales: [["short", 8, 1.0], ["medium", 32, 1.0]]
max_depth: 16
dropout: 0.0
reconstruction: true
residual_dynamics: true
normalize_q: true
use_tree_context: true
use_depth_embedding: true
flatk_max: 256
